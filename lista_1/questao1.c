#include <stdio.h>

int main(){
int a, b, c; //Declarando as variaveis que serao obtidas

  scanf("%d %d %d", &a, &b, &c); //Recebe as variaveis

  printf("%d, %d, %d\n", a, b, c); //Imprime as variaveis

  return 0;
}
